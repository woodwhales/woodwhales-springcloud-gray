spring:
  application:
    name: gray-server1

server:
  address: 0.0.0.0
  port: 9081

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:9079/eureka/
  instance:
    prefer-ip-address: true
config:
  serverName: gray-server1-old

logging:
  level:
    cn.woodwhales: debug